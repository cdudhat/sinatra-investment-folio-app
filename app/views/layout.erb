<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <title><%= @title %></title>
  </head>

  <body>
    <div class="wrapper">

      <!-- Navigation Bar -->
      <% if logged_in? %>
        <div id="textbox">
          <p class="left">
            <a href="/home">Home</a> |
            <a href="/logout">Logout</a>
          </p>
          <p class="right">Add NEW -
            <a href="/stock/new">Stock</a> |
            <a href="/product/new">Bank Product</a> |
            <a href="/fund/new">Retirement Fund</a> |
            <a href="/property/new">Investment Property</a>
          </p>
        </div>
        <div style="clear: both;"></div>
          <p class="right">Current Total Investments Value: $<%= current_user.total_value %></p>
        <div style="clear: both;"></div>
      <% end %>

      <!-- Title & Sub-title -->
      <h1>Investment Folio Manager</h1>
      <% if @heading %>
        <h2><%= @heading %></h2>
      <% else %>
        <h2><%= @title %></h2>
      <% end %>

      <!-- Flash Message -->
      <div class="flash">
        <% if flash.has?(:message) %>
          <%= flash[:message] %>
          <br /><br />
        <% end %>
      </div>

        <%= yield %>

      <!-- Footer -->
      <footer class="branding">
        <small>&copy; 2017 <strong>SinatraInvestmentFolioApp</strong></small>
      </footer>

    </div>
  </body>
</html>
